# Commit-pinned gitleaks fingerprints. Each line grants exactly one finding
# in exactly one commit; editing or moving the content re-triggers the scan,
# which is the property that makes this the narrow escape hatch
#

# A temporary fake credential, planted to prove the
# Terraform scanner catches a hardcoded secret, removed in the next commit,
# permanent in history. The string is a hand-typed hex pattern, not a real
# token; see commit 85f4ebb's own message.
85f4ebb038681cc2d690669f00f0f1049ca200c0:deploy/terraform/environments/dev/variables.tf:github-pat:4
